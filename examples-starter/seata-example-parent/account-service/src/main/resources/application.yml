server:
  port: 8083

spring:
  application:
    name: account-service
  cloud:
    nacos:
      discovery:
        server-addr: 10.255.1.8:8848
        group: seata
        namespace: cloud
        username: cloud
        password: cloud@2020
      config:
        server-addr: 10.255.1.8:8848
        group: seata
        namespace: cloud
        username: cloud
        password: cloud@2020
        file-extension: yaml
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://10.255.1.8:3306/seata_account?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: cqt@1234

seata:
  enabled: true
  application-id: ${spring.application.name}
  tx-service-group: my_test_tx_group
  service:
    vgroup-mapping:
      my_test_tx_group: default
  registry:
    type: nacos
    nacos:
      application: seata-server
      server-addr: 10.255.1.8:8848
      namespace: cloud
      group: seata
      username: cloud
      password: cloud@2020
  config:
    type: nacos
    nacos:
      server-addr: 10.255.1.8:8848
      namespace: cloud
      group: seata
      username: cloud
      password: cloud@2020

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
