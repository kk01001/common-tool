spring:
  application:
    name: examples
  cloud:
    compatibility-verifier:
      enabled: false
    nacos:
      server-addr: 10.255.1.8:8848
      username: test
      password: test
      discovery:
        namespace: public
        group: public
      config:
        file-extension: yaml
        namespace: public
        group: public
#        extension-configs:
#          - data-id: examples-db.yaml
#            group: test
#            refresh: true
