spring:
  application:
    name: examples
  cloud:
    compatibility-verifier:
      enabled: false
    nacos:
      #      server-addr: 127.0.0.1:8848
      server-addr: 10.255.1.8:8848
      username: test
      password: test
      discovery:
        namespace: test
        group: test
      config:
        file-extension: yaml
        namespace: test
        group: test
  #        extension-configs:
  #          - data-id: examples-db.yaml
  #            group: test
  #            refresh: true
#  config:
#    import: optional:nacos:examples-db.yaml?group=test&refreshEnabled=true