spring:
  application:
    name: examples
  cloud:
    compatibility-verifier:
      enabled: false
    nacos:
      server-addr: 127.0.0.1:8848
      username: nacos
      password: nacos
      discovery:
        namespace: test
        group: test
      config:
        file-extension: yaml
        namespace: test
        group: test
        extension-configs:
          - data-id: examples-db.yaml
            group: test
            refresh: true
